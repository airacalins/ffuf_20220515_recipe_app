import 'package:flutter/material.dart';

class FormImageInput extends StatelessWidget {
  const FormImageInput({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
